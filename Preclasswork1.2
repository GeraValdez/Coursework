import random
lst = []
for x in range (0, 10):
    lst.append(random.randint(0, 100))
print lst

def insertion_sort(lst):
    pos = 0
    while pos < len(lst) and lst[]
    for i in range (len(lst)):
        if lst[pos] > lst[pos+1]:
            
        if lst[i] > lst[i+1]:
            lst[i]
    lst[0]
