def cut_rod(p, n):
    if n == 0: 
        return 0
    currentmax = -float('inf')
    for j in range (1, n+1):
        currentmax = max(currentmax, p[j] + cut_rod(p,n-j))
    return currentmax
    
p=[2,4,5,7,8,9,13,14,14,19,20,21,22,23,26,28,29,30]

r = CutRod(p,16)
print ("Max revenue:", r)
